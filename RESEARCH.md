# research/math-tuning

This branch is the Math Lab — R&D only, never merges directly to main.

## Purpose
- Python DSP algorithm tuning
- Math verification scripts  
- New coefficient research
- Alternative algorithm prototypes

## Workflow
1. Experiment here freely
2. When results are proven → open a PR to main with only the outcome
3. Never merge raw experiments to main

## Contents
- `tools/research/` — Python scripts
- `tests/fixtures/generate_*.py` — fixture generators
- Any experimental Rust code

## Status
Idle — activate when DSP algorithm tuning is needed.
