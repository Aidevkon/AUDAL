[package]
name        = "adapter-runtime"
version     = "0.1.0"
edition     = "2021"
# Ιδιωτικό, εμπορικό. Η παραχώρηση αδείας είναι
# απόφαση του ιδιοκτήτη — ΟΧΙ προεπιλογή εργαλείου.
# (2026-09-06· repo ιδιωτικό, μηδέν δημοσιεύσεις)
license = "UNLICENSED"
publish = false
description = "Creator OS — LLM Adapter Runtime. Sole permitted LLM invocation point."

# Authority: LLM Adapter Amendment v1.1 §A4
# This crate is the ONLY permitted point for LLM API calls in the system.
# No adapter, app, or pipeline may call an LLM API directly.

[dependencies]
reqwest  = { version = "0.12", features = ["json"] }
serde    = { version = "1",    features = ["derive"] }
serde_json = "1"
tokio    = { version = "1",    features = ["full"] }
